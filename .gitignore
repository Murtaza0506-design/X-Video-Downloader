# macOS system file
.DS_Store

# Downloaded videos — don't publish these to the repo
downloads/

# Python cache files
__pycache__/
*.pyc

# Your personal download history — not for the public repo
history.json

# Node tooling used for local screenshot checks
node_modules/
package.json
package-lock.json

# print build intermediates; the PDFs and the spec are committed, the HTML is not
print/*.html
ebook/cover.html
